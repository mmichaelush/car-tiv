# כללי עבודה בקוד

## לפני שמוסרים

```bash
npm run verify
```

פורמט, lint, טיפוסים ובדיקות. אותו דבר רץ ב-CI, אז אם זה עובר מקומית זה יעבור שם.

## שכבות

הכיוון תמיד יורד, אף פעם לא עולה:

```
UI  →  feature  →  repository  →  HTTP  →  route  →  service  →  repository  →  D1
```

- רכיב UI לא יודע מה זה endpoint.
- route לא יודע מה זה עמודה בטבלה.
- repository לא יודע מה זה Response.

אם קובץ צריך לעלות שכבה — סימן שהוא בשכבה הלא נכונה.

## כתיבה

- **כל פונקציה ציבורית מתועדת** — לא "מה היא עושה" אלא **למה היא קיימת** ומה
  ההחלטה שמאחוריה. הערה שחוזרת על שם הפונקציה היא רעש.
- **שמות מלאים.** `video` ולא `v`, `manufacturer` ולא `mfg`.
- **פונקציה שעושה דבר אחד.** אם צריך "וגם" כדי לתאר אותה — לפצל.
- **קובץ מעל ~400 שורות** הוא סימן שיש בו שני דברים.
- **טיפוסים מפורשים בגבולות** (פרמטרים, ערכי החזרה). בפנים — הסקה.
- **`readonly` בברירת מחדל** בטיפוסי נתונים.
- **אין `any`.** יש `unknown` ואחריו narrowing.
- **אין `!`** (non-null assertion). יש `select()` שזורק עם הודעה מובנת.

## שגיאות

- ב-Worker: זורקים `HttpError` מ-`worker/lib/errors.ts`. יש **מקום אחד** שהופך
  שגיאה לתשובה — `middleware/error-handler.ts`.
- בדפדפן: `ApiError` נושא `code`, `status` ו-`fields`. מסתעפים לפי `code`,
  אף פעם לא לפי טקסט ההודעה.
- הודעה למשתמש היא בעברית ומסבירה מה לעשות. הודעה ללוג היא באנגלית ומסבירה מה קרה.

## פיצ'ר נחשב גמור כאשר

- [ ] עובד ב-Desktop וב-Mobile
- [ ] RTL תקין
- [ ] נבדק בערכת נושא בהירה וכהה
- [ ] יש מצב טעינה (skeleton, לא ספינר על כל העמוד)
- [ ] יש מצב ריק עם דרך יציאה
- [ ] יש מצב שגיאה עם "נסו שוב"
- [ ] נגיש במקלדת, עם focus נראה
- [ ] ולידציה בשרת (לא רק בדפדפן)
- [ ] יש בדיקה
- [ ] אין שגיאות בקונסול
- [ ] לא נוספה שאילתה מיותרת לכל טעינת עמוד
- [ ] כתובת קיימת לא נשברה בלי redirect

## אבטחה — לא לוותר על אלה

- כל ערך מהמשתמש או מה-DB עובר escaping לפני שהוא מגיע ל-DOM.
- כל SQL עם bindings.
- כל כתיבה עוברת בדיקת origin ו-rate limit.
- כל endpoint ב-`/api/admin` מתחיל ב-`requireStaff`.
- סוד לא נכנס ל-git, לא לקוד צד לקוח ולא ללוג.
