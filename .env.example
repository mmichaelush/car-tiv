# Copy to `.env.local` for a local build with analytics or other build-time
# configuration. `.env.local` is git-ignored; `.env.production` is committed
# because everything in it is public.

VITE_GTM_ID=
