# מדריך לסוכן AI

המסמך הזה נכתב כדי שסוכן שנכנס לפרויקט יידע **איפה הקוד שייך** בלי לקרוא הכול,
ו**מה אסור** בלי לגלות את זה בבדיקות.

קראו קודם: [`docs/architecture.md`](docs/architecture.md), ולפני שמוחקים או
מחליפים משהו — [`docs/regression-contract.md`](docs/regression-contract.md).
המסמך השני הוא הרשימה המחייבת של מה שהאתר עשה לפני השכתוב ואיפה כל דבר נמצא
היום; יכולת לא נעלמת בלי שורה שמנמקת את זה שם.

---

## איפה לשים כל דבר

| הוספתם…                          | הקוד הולך ל…                                                           |
| -------------------------------- | ---------------------------------------------------------------------- |
| חישוב טהור (נרמול, פורמט, דירוג) | `shared/core/` + בדיקה ב-`tests/shared/`                               |
| שדה חדש שה-API מחזיר             | `shared/types/catalog.ts` ואז ה-repository בשני הצדדים                 |
| endpoint חדש                     | `worker/routes/<תחום>-routes.ts` + רישום ב-`worker/routes/index.ts`    |
| שאילתה חדשה                      | `worker/repositories/` — ורק שם                                        |
| לוגיקה שמורכבת מכמה repositories | `worker/services/`                                                     |
| ולידציה של גוף בקשה              | `worker/schemas/index.ts` (zod), עם הודעה בעברית לכל שדה               |
| טבלה או עמודה                    | `migrations/NNNN_*.sql` חדש. **לעולם לא לערוך migration קיים**         |
| רכיב ויזואלי                     | `src/ui/components/` — פונקציה טהורה שמחזירה `SafeHtml`                |
| פיצ'ר שלם                        | `src/features/<שם>/`                                                   |
| עמוד                             | `<שם>/index.html` בשורש + `src/app/<שם>.ts` + רישום ב-`vite.config.ts` |
| צבע, מרווח, רדיוס                | `src/styles/tokens.css` — לא בתוך רכיב                                 |
| מספר מצטבר שמוצג למבקר           | עמודה ב-`0008_counters` + רענון ב-`CountersRepository` — לא `COUNT(*)` |

## מה אסור — הכללים שמייצרים באגים כשעוברים עליהם

1. **אין `fetch` ברכיב UI.** רק `src/data/*-repository.ts`. ESLint אוכף.
2. **אין SQL מחוץ ל-`worker/repositories/`.**
3. **אין ערך שמגיע מהמשתמש או מה-DB בתוך `innerHTML` בלי `html\`\``** מ-`src/ui/dom.ts`.
התבנית הזו עושה escaping לכל ערך שמוכנס לה. `raw()` הוא ויתור מודע — רק לתוכן
   שכתוב בקוד עצמו (אייקונים).
4. **אין `localStorage` מחוץ ל-`src/data/local-store.ts`.** כל גישה עטופה ב-try/catch:
   בגלישה פרטית האחסון זורק.
5. **אין שינוי schema בלי migration.**
6. **אין ערך שנכנס ל-SQL בשרשור מחרוזות.** רק bindings — `ConditionBuilder`
   מוסיף תנאי ו-binding יחד כדי שאי אפשר יהיה לשכוח אחד מהם.
7. **אין hostname קשיח בקוד.** `env.APP_URL` או `window.location.origin`.
8. **אין `console.log`.** ב-Worker: `context.logger`. בדפדפן: `toast` או `console.error`.
9. **אין endpoint חדש לכל כפתור.** מסננים נוספים נכנסים ל-`VideoQuery`.
10. **אין כתיבה ל-DB על כל אירוע קטן.** התקדמות צפייה נשמרת מקומית ב-debounce.
11. **אין `COUNT(*)` או `GROUP BY` על טבלה שגדלה, בנתיב שמבקר עובר בו.**
    זו לא העדפת סגנון: `/api/tags` עשה בדיוק את זה ועלה ~127,000 שורות לבקשה,
    כלומר 39 בקשות ליום עד סוף המכסה. מספר מצטבר שמוצג למבקר הוא עמודה מתוחזקת.
    `tests/worker/query-cost.test.ts` נכשל אם חוזרים לזה.
12. **אין שמירה במטמון של תשובה שיש בה `Set-Cookie`.** שכבת המטמון בודקת את זה,
    ואסור להחליש את הבדיקה: תשובה עם עוגיית session שנשמרת מוסרת אותה למבקר הבא.
13. **עוגיית ה-hint (`cartiv_auth`) אינה אישור גישה.** שום דבר בשרת לא קורא
    אותה. אם משהו בשרת מתחיל להסתמך עליה — זו פרצה, לא קיצור דרך.
14. **כל שינוי בכותרת, בתיאור, בתגיות, בערוץ או ברכב חייב לקרוא ל-`reindex`.**
    `videos_fts` היא טבלה עצמאית ושום דבר לא מעדכן אותה לבד. בלי זה עורך משנה
    כותרת, האתר מציג את החדשה והחיפוש ממשיך למצוא לפי הישנה — לתמיד.
    `tests/worker/search-index.test.ts` נכשל אם מסלול ניהול מפסיק לעשות את זה.
15. **הערה בקוד מתארת את מה שהקוד עושה עכשיו.** כוונה עתידית מסומנת `TODO`
    או `PLANNED` במפורש. תיעוד שמקדים את הקוד גורם לקורא הבא — אדם או סוכן —
    להניח שההבטחה נכונה. זה כבר קרה כאן: מיגרציה 0001 הפנתה ל-service לתחזוקת
    אינדקס החיפוש שמעולם לא נכתב, ואף מסלול לא קרא לו.

## איך יודעים ששינוי לא שבר כלום

```bash
npm run verify      # פורמט + lint + טיפוסים + 504 בדיקות
```

`npm run typecheck` מריץ חמישה פרויקטים נפרדים — דפדפן, Worker, סקריפטים,
בדיקות Worker, ובדיקות UI — כי לכל אחד יש ספריית טיפוסים גלובלית אחרת.
בפרט `@cloudflare/workers-types` ו-`lib.dom` מגדירים שניהם `Request`/`Response`,
ולכן `tests/ui/**` יושב ב-`tsconfig.test-ui.json` וכל שאר הבדיקות ב-`tsconfig.test.json`.
אם הוספתם קובץ, ודאו שהוא נכלל באחד מהם.

בדיקות ה-Worker רצות מול **SQLite אמיתי** עם המיגרציות האמיתיות
(`tests/helpers/d1.ts`), כך ששאילתה שגויה נופלת בבדיקה ולא ב-production.

## דברים שקל לפספס

- **עברית בחיפוש**: כל טקסט שנכנס לאינדקס וכל שאילתה עוברים דרך `indexText`.
  אם השוואתם מחרוזת גולמית למחרוזת מנורמלת — זה באג.
- **תאריכים**: `DD/MM/YYYY` הוא יום-קודם. `new Date('03/04/2024')` יקרא את זה
  כ-4 במרץ. תמיד `parseCatalogDate`.
- **סדר נתיבים**: `/api/videos/exists` חייב להירשם לפני `/api/videos/:id`.
- **`bm25` מחזיר מספר שלילי** — ככל שנמוך יותר, רלוונטי יותר. לכן `ORDER BY … ASC`.
- **הטקסט המשפטי**: `/privacy/` ו-`/terms/` נבנים מ-`data/legacy/` בסקריפט.
  אין לערוך אותם ידנית; יש בדיקה שמשווה כל מילה.
- **`s-maxage` לבדו לא עושה כלום.** תשובה שנוצרה ב-Worker אינה נשמרת במטמון של
  Cloudflare אלא אם ה-Worker שומר אותה שם — זה מה ש-`worker/middleware/edge-cache.ts`
  עושה. ההערה הישנה בקוד טענה אחרת, ובגלל זה כל מבקר הגיע ל-D1.
- **צורת ה-join משנה יותר מהאינדקסים.** ב-`findRelated`, סינון על
  `manufacturer_id` דרך join גרם ל-SQLite לסרוק את כל 7,876 השורות; פתרון
  הדגמים לרשימת מזהים קודם הוריד את אותו צעד פי 40. כשמוסיפים שאילתה, מסתכלים
  ב-`EXPLAIN QUERY PLAN` ולא רק בשמות האינדקסים.
- **`EXISTS` מול `IN` בסינון תגית**: `EXISTS` זול לתגית נפוצה ויקר לתגית נדירה,
  ו-`IN` להפך. כמעט כל התגיות נדירות, ולכן `IN`. הנימוק המלא ב-`video-repository.ts`.
- **תקציב המשאבים**: [`docs/performance.md`](docs/performance.md) הוא הרשומה
  המחייבת של מה כל endpoint עולה ולמה. שינוי שמייקר endpoint שייך לשם.
- **`slugify` ≠ `indexText`**: `slugify` שומר אותיות סופיות, `indexText` מקפל
  אותן (ם→מ) לצורך חיפוש. כל מקום שמחפש תגית לפי slug חייב להשתמש ב-`slugify`,
  אחרת כל תגית שמסתיימת ב-ם/ן/ץ/ף/ך פשוט לא תימצא. זה היה באג אמיתי ב-`removeTag`.
- **rate limiter קורא לפני שהוא כותב**: ספירה של בקשה חסומה היא כתיבה, ומכסת
  הכתיבה זהה למכסת הבקשות. מנגנון שמאפשר להצפה לשרוף את המכסה שהוא אמור להגן
  עליה אינו מגן על כלום.
